// interfaces used in the code

export enum ButtonId {
    PLAY = 'playBtn',
    START = 'startBtn',
    ACCOUNTANT = 'accountantBtn',
    LAWN = 'lawnBtn',
}

export enum Side {
    WORK = 'work',
    LIFE = 'life'
}

export enum GameState {
    BEFORE = 0,
    PLAYING = 1,
    AFTER = 2
}