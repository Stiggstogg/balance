// interfaces used in the code

export enum ButtonId {
    PLAY = 'playBtn',
    START = 'startBtn'
}

export enum Side {
    WORK = 'work',
    LIFE = 'life'
}

export enum GameState {
    BEFORE = 0,
    PLAYING = 1,
    AFTER = 2
}